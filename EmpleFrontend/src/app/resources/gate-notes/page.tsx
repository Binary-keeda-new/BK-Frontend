import GateNotesHome from "@/features/user/resources/pages/GateNotesHome";

export const metadata = {
  title: 'GATE Computer Science Notes | Emple',
  description: 'Free handwritten GATE CS notes for all core subjects.',
};

export default function GateNotesPage() {
  return <GateNotesHome />;
}
